# Dynamic theme colors
config-file = ?"~/.config/omarchy/current/theme/ghostty.conf"

# Font
font-family = "Maple Mono NF CN"
font-style = Regular
font-size = 9

# Window
window-theme = ghostty
window-padding-x = 14
window-padding-y = 14
confirm-close-surface=false
resize-overlay = never
gtk-toolbar-style = flat

# Cursor styling
cursor-style = "block"
cursor-style-blink = false

# Cursor styling + SSH session terminfo
# (all shell integration options must be passed together)
shell-integration-features = no-cursor,ssh-env

# Keyboard bindings
keybind = shift+insert=paste_from_clipboard
keybind = control+insert=copy_to_clipboard
# Ctrl+V pastes. Wispr Flow injects dictated text by copying to the clipboard and
# sending Ctrl+V; terminals ignore Ctrl+V by default, so this lets dictation land
# in Ghostty (mirrors the same binding in alacritty.toml).
keybind = ctrl+v=paste_from_clipboard
# Ctrl+Alt+V sends a raw Ctrl+V byte through to the running app (instead of Ghostty
# pasting), so TUIs like Claude Code receive Ctrl+V for their own paste — e.g.
# pasting a clipboard image. Ctrl+V = paste text (dictation), Ctrl+Alt+V = app paste.
keybind = ctrl+alt+v=text:\x16
# Send Shift+Enter as CSI-u so TUIs can distinguish it from Enter.
keybind = shift+enter=csi:13;2u
# Legacy encoding sends Alt+Shift+Enter the same as Alt+Enter; send CSI-u so tmux can match M-S-Enter.
keybind = alt+shift+enter=csi:13;4u
keybind = super+control+shift+alt+arrow_down=resize_split:down,100
keybind = super+control+shift+alt+arrow_up=resize_split:up,100
keybind = super+control+shift+alt+arrow_left=resize_split:left,100
keybind = super+control+shift+alt+arrow_right=resize_split:right,100

# Slowdown mouse scrolling
mouse-scroll-multiplier = 0.95

# Fix general slowness on hyprland (https://github.com/ghostty-org/ghostty/discussions/3224)
async-backend = epoll

# Alt+Backspace -> ESC DEL: delete previous word (readline backward-kill-word / Claude Code delete-word)
keybind = alt+backspace=text:\x1b\x7f
# Super+Backspace -> Ctrl+U: delete to line start, like macOS Cmd+Backspace
keybind = super+backspace=text:\x15
