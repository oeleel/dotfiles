# See https://wiki.hypr.land/Configuring/Basics/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Optimized for retina-class 2x displays, like 13" 2.8K, 27" 5K, 32" 6K.
# env = GDK_SCALE,2
# monitor=,preferred,auto,auto

# Straight 1x for the 1440p + 1080p monitors below
env = GDK_SCALE,1

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.6

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# Or for ultrawide monitors like 34" 3440x1440 or 49" 5120x1440
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Portrait/rotated secondary monitor (transform: 1 = 90°, 3 = 270°)
# monitor = DP-2, preferred, auto, 1, transform, 1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2

# Disable the second ghost monitor on an Apple 6K XDR over Thunderbolt
# monitor=DP-2,disable


# Mixed-DPI compromise: 27"/1440p = 4.36 px/mm vs 24"/1080p = 3.6 px/mm, so no
# single offset matches the physical edge everywhere. y=360 aligns pixel
# bottoms but makes top crossings feel ~50mm too strict; y=131 matches
# physical tops but dead-zones the bottom ~230px. y=245 splits the error
# (~26mm at each extreme). Tune toward 131 if top crossings still feel low,
# toward 360 if bottom crossings feel blocked.
monitor = HDMI-A-1, 1920x1080@144, 0x245, 1
monitor = DP-3, 2560x1440@180, 1920x0, 1
